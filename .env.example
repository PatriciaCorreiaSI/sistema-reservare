# Chaves existentes sem revelar valores

POSTGRES_USER=
POSTGRES_PASSWORD=
POSTGRES_DB=
# No host, use 127.0.0.1, não localhost: o Windows resolve localhost
# para ::1 primeiro, e o compose publica a porta só em IPv4
DB_HOST=
